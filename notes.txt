now create a new node called block_extraction_scipting. this node create a folder in the agent workspace called extraction_scriots. once this is done I want to start a node for each block_id (so in parallel) where a codex instance is started within the block sample folder with the instruction to create a python script to extract data from that block, the script must be generalizable to all the instance of that block type
